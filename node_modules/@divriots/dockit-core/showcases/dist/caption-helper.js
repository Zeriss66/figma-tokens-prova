const getCaption = (showcaseClass, showcaseStyle) => {
  if (showcaseClass)
    return showcaseClass;
  return getValue(showcaseStyle);
};
const getValue = (style) => {
  const [_, val] = style.split(":");
  return (val || "").trim().replace(/var\(|\)/gi, "");
};
export { getCaption, getValue };
