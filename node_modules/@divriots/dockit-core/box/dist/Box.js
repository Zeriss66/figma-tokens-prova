import { checkeredBackground } from "./Box.styles.js";
const wrapper = "_wrapper_6cez5_1";
var styles = {
  wrapper
};
class Box extends HTMLElement {
  connectedCallback() {
    const hasCheckedBackground = this.hasAttribute("checkered-background");
    const className = this.getAttribute("class-name") || "";
    const style = this.getAttribute("showcase-style") || "";
    this.innerHTML = `
      <div class="${styles.wrapper}">
        ${hasCheckedBackground ? `<div style="${style}; ${checkeredBackground}" class="${className}"></div>` : ""}
        <div class="${className}" style="${style}"></div>
      </div>
      `;
  }
}
export { Box };
